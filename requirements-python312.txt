# Optional stack for Python 3.12+ (the original requirements.txt pins NumPy 1.19
# and TensorFlow 2.5, which do not install on 3.12). Use this for local runs.
#
# Install (from repo root, venv active):
#   pip install -r requirements-python312.txt
#
# TensorFlow is only needed if you use the L2X explainer; it is not imported
# until you select that explainer (lazy import in custom_explainers).

commentjson>=0.9.0
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.64.0
dill>=0.3.8
shap>=0.44.0
